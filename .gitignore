__pycache__/
*.egg-info/
build/
dist/

# 測試產生的音檔（用 tests/run_tests.ps1 重新產生）
tests/*.wav
tests/*.mp3
tests/*.m4a
tests/*.srt
tests/*.txt
tests/*.json
tests/batch/

# 網頁精靈的工作區與成品
workspace/
成品/

# 本機密鑰（雲端部署的密碼設定在 Streamlit Cloud 的 Secrets 後台，不進版控）
.streamlit/secrets.toml

# DeepFilterNet 的本機 Python 3.11 環境（用 setup_dfn.ps1 重建）
.dfn311/

# F5-TTS 語音克隆環境（用 setup_clone.ps1 重建）
.clone311/
tests/clone_out/

# 克隆參考音（由 setup_clone.ps1 從 F5-TTS 內附語音產生，不進版控）
voices/
